# 🔄 Обновление дашборда на сервере

## Проблема
Старый светлый `index.html` уже скопирован в образ frontend при сборке.

## Быстрое решение (без пересборки frontend)

### Вариант 1: Обновить файл напрямую в контейнере

```bash
# 1. Скопируйте новый index.html в контейнер
docker cp tests/public/autotest/index.html ebuster-frontend:/usr/share/nginx/html/autotest/index.html

# 2. Перезапустите Nginx
docker exec ebuster-frontend nginx -s reload

# 3. Очистите кэш браузера и обновите страницу
# Ctrl+Shift+R или Cmd+Shift+R
```

### Вариант 2: Использовать volume (рекомендуется)

Дашборд уже монтируется через volume `autotest_reports`, но плейсхолдер был в образе.

```bash
# 1. Дождитесь завершения тестов
# Скрипт run-all-tests.ts автоматически сгенерирует новый index.html

# 2. Или сгенерируйте вручную
docker compose run --rm autotest-runner npm run test:dashboard:live

# 3. Проверьте что файл обновился
docker exec ebuster-frontend cat /usr/share/nginx/html/autotest/index.html | head -20
```

## Полное решение (с пересборкой)

```bash
cd /srv/ebuster

# 1. Получите изменения
git pull

# 2. Остановите контейнеры
docker compose down

# 3. Пересоберите только frontend (быстрее чем --no-cache)
docker compose build frontend

# 4. Запустите
docker compose up -d

# 5. Проверьте
curl -s https://autotest.ebuster.ru | grep "Dark Graphite"
```

## Проверка текущего дизайна

```bash
# Проверьте CSS переменные в index.html
docker exec ebuster-frontend grep -A 10 ":root" /usr/share/nginx/html/autotest/index.html

# Должно быть:
# --background: #1a1a1a;
# --foreground: #d9d9d9;
# --card: #202020;
```

## Если тесты уже запущены

Тесты сейчас работают, просто дождитесь их завершения:

```bash
# Следите за прогрессом
tail -f /srv/ebuster/run-tests-and-deploy.sh

# Или смотрите логи stream-сервера
docker compose logs -f autotest-stream
```

После завершения тестов `generate-live-dashboard.ts` автоматически создаст новый тёмный дашборд и перезапишет старый плейсхолдер.

## Исправление ошибки stream-сервера

Видно в логах:
```
Failed to send update to stream server: TypeError: fetch failed
```

Это потому что `autotest-stream` ещё не запущен или недоступен. Проверьте:

```bash
# Проверьте статус
docker compose ps autotest-stream

# Проверьте логи
docker compose logs autotest-stream

# Проверьте healthcheck
curl http://localhost:3002/status

# Если не работает, перезапустите
docker compose restart autotest-stream
```

## После исправления

1. ✅ Дождитесь завершения текущих тестов
2. ✅ Новый дашборд будет сгенерирован автоматически
3. ✅ Обновите страницу с Ctrl+Shift+R
4. ✅ Должен быть тёмный стиль

---

**Или просто скопируйте файл прямо сейчас:**

```bash
docker cp tests/public/autotest/index.html ebuster-frontend:/usr/share/nginx/html/autotest/index.html
docker exec ebuster-frontend nginx -s reload
```

И обновите браузер с очисткой кэша!
