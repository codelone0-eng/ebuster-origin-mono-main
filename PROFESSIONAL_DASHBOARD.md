# 🎯 Профессиональный дашборд автотестов

## Что изменено

### ✅ Дизайн
- **Убраны все эмодзи** → профессиональные SVG иконки
- **Точная копия стиля** основного сайта (Dark Graphite Theme)
- **Минималистичная цветовая схема** - только серые тона + статусные цвета
- **Профессиональная типографика** - Inter, моноширинный шрифт для логов

### ✅ Функциональность
- **Кнопка "Запустить тесты"** - запуск прямо из дашборда
- **Real-time обновления** - WebSocket подключение к stream-серверу
- **Live логи** - видно что происходит в реальном времени
- **Автообновление** - дашборд обновляется автоматически

### ✅ Новые файлы
1. `tests/scripts/generate-professional-dashboard.ts` - новый генератор дашборда
2. Обновлён `tests/stream-server/index.js` - добавлен POST /run endpoint
3. Обновлён `package.json` - использует новый скрипт

## После автодеплоя на сервере

```bash
cd /srv/ebuster

# Дождитесь автодеплоя через GitHub Actions

# Удалите старый volume
docker compose down
docker volume rm ebuster_autotest_reports

# Запустите
docker compose up -d

# Сгенерируйте новый дашборд
docker compose run --rm autotest-runner npm run test:dashboard:live

# Или запустите тесты (дашборд создастся автоматически)
docker compose run --rm autotest-runner npm run test:all
```

## Использование

### Запуск тестов из дашборда

1. Откройте https://autotest.ebuster.ru
2. Нажмите кнопку **"Запустить тесты"**
3. Подтвердите запуск
4. Наблюдайте за выполнением в реальном времени

### Live-обновления

- **Статус** - показывает текущее состояние (Ожидание / Тесты выполняются / Завершено)
- **Статистика** - обновляется в реальном времени
- **Логи** - каждый тест отображается сразу после выполнения
- **Автоскролл** - логи автоматически прокручиваются вниз

## Дизайн

### Цвета (только из основного сайта)
- **Фон**: `#1a1a1a`
- **Карточки**: `#202020`
- **Границы**: `#606060`
- **Текст**: `#d9d9d9`
- **Приглушённый**: `#808080`
- **Успех**: `#10b981`
- **Ошибка**: `#e06666`
- **Предупреждение**: `#f59e0b`

### Иконки
- Все иконки - **SVG** (Heroicons style)
- Никаких эмодзи
- Профессиональный вид

### Типографика
- **Основной шрифт**: Inter
- **Моноширинный**: SF Mono, Monaco, Cascadia Code
- **Размеры**: 13px-36px
- **Веса**: 400, 500, 600, 700

## API

### GET /stream/status
Получить текущее состояние тестов

```bash
curl https://autotest.ebuster.ru/stream/status
```

### POST /stream/run
Запустить тесты

```bash
curl -X POST https://autotest.ebuster.ru/stream/run
```

### WebSocket /ws
Подключение для live-обновлений

```javascript
const ws = new WebSocket('wss://autotest.ebuster.ru/ws');
ws.onmessage = (event) => {
  const { type, data } = JSON.parse(event.data);
  console.log(type, data);
};
```

## Проверка

```bash
# Проверьте что дашборд обновлён
curl -s https://autotest.ebuster.ru | grep "Запустить тесты"

# Должно вернуть кнопку

# Проверьте stream-сервер
curl https://autotest.ebuster.ru/stream/status

# Должно вернуть JSON с состоянием

# Проверьте WebSocket
# Откройте https://autotest.ebuster.ru
# В консоли браузера должно быть: "Connected to live stream"
```

## Структура дашборда

```
┌─────────────────────────────────────────────────────┐
│ Header                                              │
│ - Заголовок                                         │
│ - Статус индикатор                                  │
│ - Кнопка "Запустить тесты"                          │
└─────────────────────────────────────────────────────┘

┌──────────┬──────────┬──────────┬──────────┐
│ Всего    │ Успешно  │ Провалено│ Пропущено│
│ [icon]   │ [icon]   │ [icon]   │ [icon]   │
│   105    │    33    │    72    │     0    │
└──────────┴──────────┴──────────┴──────────┘

┌─────────────────────────────────────────────────────┐
│ UI Admin Panel                        [Провалено]   │
│ [✓] 16  [✗] 40  [→] 0  [⏱] 851s                    │
│ Подробный отчёт →                                   │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ Логи выполнения                      [Очистить]     │
│ 09:11:43  Starting test run...                      │
│ 09:11:44  Running UI Admin Panel...                 │
│ 09:11:45  Test passed: login works                  │
└─────────────────────────────────────────────────────┘
```

---

**Теперь дашборд выглядит профессионально и работает в реальном времени!** 🎯
