# ⚡ Быстрый деплой исправлений

## На сервере выполните:

```bash
# 1. Перейдите в директорию
cd /srv/ebuster

# 2. Получите изменения
git pull origin main

# 3. Остановите контейнеры
docker compose down

# 4. Пересоберите образы
docker compose build --no-cache

# 5. Запустите сервисы
docker compose up -d

# 6. Проверьте что всё запустилось
docker compose ps

# 7. Запустите тесты
docker compose run --rm autotest-runner npm run test:all
```

## Что исправлено:

### ✅ Playwright версия
- Обновлён образ с `v1.48.2` на `v1.56.1`
- Теперь тесты будут запускаться без ошибок

### ✅ Тёмный стиль дашборда
- Полностью переделан в Dark Graphite Theme
- Точная копия основного сайта:
  - Тёмный фон `#1a1a1a`
  - Карточки `#202020`
  - Границы `#606060`
  - Цвета как на основном сайте
  - Шрифт Inter
  - Тени и отступы 1в1

### ✅ WebSocket подключение
- Добавлен `map $http_upgrade $connection_upgrade`
- Увеличены таймауты для WebSocket
- Исправлена конфигурация Nginx

## Проверка результата:

```bash
# Проверьте stream-сервер
curl http://localhost:3002/status

# Проверьте логи
docker compose logs -f autotest-stream

# Откройте в браузере
https://autotest.ebuster.ru
```

## Если WebSocket всё ещё не работает:

```bash
# Проверьте что порт 3002 открыт
netstat -tulpn | grep 3002

# Проверьте firewall
sudo ufw allow 3002/tcp

# Перезапустите Nginx
docker compose restart frontend

# Проверьте логи Nginx
docker compose logs frontend | grep -i websocket
```

## Cloudflare настройки:

Если используете Cloudflare:
1. Зайдите в панель Cloudflare
2. Network → WebSockets → **Включите**
3. Или временно отключите проксирование для `autotest.ebuster.ru`

---

**После деплоя дашборд будет:**
- 🌑 Тёмный как основной сайт
- 🔴 С live-обновлениями через WebSocket
- ✅ Без ошибок Playwright
