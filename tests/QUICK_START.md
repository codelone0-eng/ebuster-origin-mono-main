# ⚡ Quick Start - Ebuster Testing

## Одна команда для всего

```bash
# 1. Экспортировать переменные окружения
export $(grep -v '^#' .env.autotest | xargs)

# 2. Запустить все тесты
npm run test:all

# 3. Сгенерировать дашборд
npm run test:dashboard

# 4. Открыть веб-сервер
npm run test:serve
```

Откройте браузер: **http://localhost:8888**

---

## Что будет запущено?

### 1. 👨‍💼 UI Admin Panel (admin.ebuster.ru)
- Управление пользователями
- Управление скриптами
- Управление подписками
- Управление тикетами
- Управление рефералами
- Навигация по админке

### 2. 👤 UI User Dashboard (lk.ebuster.ru)
- Дашборд пользователя
- Библиотека скриптов
- Тикеты поддержки
- Реферальная программа

### 3. 🔧 Backend Admin API
- Реферальные эндпоинты
- Скрипты пользователей
- Тикеты
- Health check

### 4. 🔌 Backend User API
- Скрипты API
- Подписки API
- Тикеты API
- Профиль пользователя

---

## Результаты

После выполнения вы получите:

### 📊 Главный дашборд
- Общая статистика по всем тестам
- Карточки для каждой категории
- Прямые ссылки на детальные отчёты

### 📁 Структура отчётов
```
tests/reports/
├── index.html              ← Главный дашборд
├── summary.json            ← JSON со всеми результатами
├── ui-admin/html/          ← Детальный отчёт UI Admin
├── ui-lk/html/             ← Детальный отчёт UI LK
├── api-admin/html/         ← Детальный отчёт API Admin
└── api-lk/html/            ← Детальный отчёт API LK
```

---

## Запуск отдельных категорий

```bash
# Только UI Admin
npm run test:ui-admin

# Только UI LK
npm run test:ui-lk

# Только Backend Admin
npm run test:api-admin

# Только Backend User
npm run test:api-lk
```

---

## Отправка в Telegram

```bash
# После запуска всех тестов
npm run autotest-send-report
```

Или одной командой:

```bash
npm run test:all && npm run autotest-send-report
```

---

## Автоматизация (Cron)

```bash
# Добавить в crontab
crontab -e

# Запускать каждый день в 3:00
0 3 * * * cd /srv/ebuster && export $(grep -v '^#' .env.autotest | xargs) && npm run test:all && npm run test:dashboard && npm run autotest-send-report
```

---

## Troubleshooting

### Тесты не запускаются?
```bash
npm install
npx playwright install --with-deps
```

### Отчёты не открываются?
```bash
# Проверьте, что сервер запущен
npm run test:serve

# Откройте http://localhost:8888
```

### Нет результатов на дашборде?
```bash
# Сначала запустите тесты
npm run test:all

# Затем сгенерируйте дашборд
npm run test:dashboard

# Затем запустите сервер
npm run test:serve
```

---

## Полная документация

Смотрите **`TESTING_GUIDE.md`** для подробной информации.
