# 📧 Диагностика проблем с отправкой писем

## 🔍 Быстрая проверка SMTP

### На сервере:

```bash
# 1. Перейти в директорию проекта
cd /srv/ebuster

# 2. Запустить тест SMTP
docker compose exec api node test-smtp.js

# Или отправить на конкретный email:
docker compose exec api node test-smtp.js your-email@example.com
```

---

## 📋 Проверка настроек

### 1. Проверить переменные окружения

```bash
# На сервере
cat /srv/ebuster/.env | grep SMTP
```

Должно быть:
```env
SMTP_HOST=smtp.timeweb.ru
SMTP_PORT=465
SMTP_USER=techsupport@ebuster.ru
SMTP_PASS=ваш_пароль
```

### 2. Проверить логи API

```bash
# Логи в реальном времени
docker compose logs -f api

# Последние 100 строк
docker compose logs --tail=100 api

# Поиск ошибок SMTP
docker compose logs api | grep -i smtp
docker compose logs api | grep -i email
docker compose logs api | grep -i "error"
```

---

## 🐛 Частые проблемы и решения

### Проблема 1: "Connection timeout" или "ETIMEDOUT"

**Причина:** Порт 465 заблокирован файрволом или провайдером.

**Решение:**

```bash
# Проверить доступность порта
telnet smtp.timeweb.ru 465

# Если не работает, попробовать альтернативный порт 2525
```

Обновить `.env`:
```env
SMTP_PORT=2525
```

Перезапустить:
```bash
docker compose restart api
```

### Проблема 2: "Invalid login" или "Authentication failed"

**Причина:** Неверный пароль SMTP.

**Решение:**

1. Проверить пароль в панели Timeweb
2. Обновить `SMTP_PASS` в `.env`
3. Перезапустить API:
   ```bash
   docker compose restart api
   ```

### Проблема 3: Письма не приходят (нет ошибок в логах)

**Причина:** Письма попадают в спам или отклоняются почтовым сервером.

**Решение:**

1. Проверить папку "Спам" в почте
2. Добавить `techsupport@ebuster.ru` в белый список
3. Проверить SPF/DKIM записи домена:
   ```bash
   dig TXT ebuster.ru
   ```

### Проблема 4: "ECONNREFUSED" или "Connection refused"

**Причина:** SMTP сервер недоступен или неверный хост.

**Решение:**

```bash
# Проверить доступность SMTP сервера
ping smtp.timeweb.ru

# Проверить DNS
nslookup smtp.timeweb.ru
```

---

## 🔧 Ручная проверка SMTP (без Docker)

### Локально на сервере:

```bash
# 1. Установить зависимости (если нужно)
npm install

# 2. Запустить тест
node test-smtp.js your-email@example.com
```

---

## 📊 Проверка отправки через API

### Тест через curl:

```bash
# Регистрация нового пользователя (должно отправить письмо)
curl -X POST https://api.ebuster.ru/api/auth/signup \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "TestPass123!",
    "name": "Test User"
  }'

# Восстановление пароля (должно отправить письмо)
curl -X POST https://api.ebuster.ru/api/auth/reset-password \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com"
  }'
```

После запроса проверить логи:
```bash
docker compose logs --tail=50 api
```

---

## 🔐 Проверка SSL/TLS

### Проверить SSL сертификат SMTP сервера:

```bash
openssl s_client -connect smtp.timeweb.ru:465 -showcerts
```

Должно показать успешное SSL подключение.

---

## 📝 Альтернативная конфигурация SMTP

Если порт 465 не работает, попробуйте STARTTLS на порту 2525:

### Обновить `src/config/smtp.config.ts`:

```typescript
export const smtpConfig = {
  host: process.env.SMTP_HOST || 'smtp.timeweb.ru',
  port: parseInt(process.env.SMTP_PORT || '2525'), // Изменить на 2525
  secure: false, // Изменить на false для STARTTLS
  auth: {
    user: process.env.SMTP_USER || 'techsupport@ebuster.ru',
    pass: process.env.SMTP_PASS || ''
  },
  tls: {
    rejectUnauthorized: false // Только для тестирования!
  }
};
```

### Обновить `.env`:

```env
SMTP_PORT=2525
```

### Пересобрать и перезапустить:

```bash
docker compose down
docker compose build api
docker compose up -d
```

---

## 🆘 Если ничего не помогает

### 1. Проверить статус SMTP в Timeweb

Зайти в панель управления Timeweb → Почта → Проверить:
- Активен ли почтовый ящик `techsupport@ebuster.ru`
- Правильный ли пароль
- Нет ли ограничений на отправку

### 2. Проверить квоты отправки

```bash
# В логах API искать сообщения о лимитах
docker compose logs api | grep -i "rate limit"
docker compose logs api | grep -i "quota"
```

### 3. Использовать альтернативный SMTP

Временно можно использовать Gmail SMTP для тестирования:

```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-gmail@gmail.com
SMTP_PASS=your-app-password
```

**Важно:** Для Gmail нужно создать App Password в настройках аккаунта.

---

## 📞 Контакты поддержки

- **Timeweb поддержка:** https://timeweb.com/ru/help
- **Документация Timeweb SMTP:** https://timeweb.com/ru/help/pages/viewpage.action?pageId=4358529

---

## ✅ Чеклист диагностики

- [ ] Проверены переменные SMTP в `.env`
- [ ] Запущен тест `test-smtp.js`
- [ ] Проверены логи API на ошибки
- [ ] Проверена доступность порта 465/2525
- [ ] Проверен пароль SMTP в панели Timeweb
- [ ] Проверена папка "Спам" в почте
- [ ] Попробован альтернативный порт (2525)
- [ ] Проверены SPF/DKIM записи домена
- [ ] Связались с поддержкой Timeweb (если нужно)
